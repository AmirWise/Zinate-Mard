Zinate Mard

این پروژه یک انیمیشن ساده و جذاب با استفاده از کتابخانه‌ی Pygame است که با کلیک روی دکمه‌ی Start، چند مرحله انیمیشن اجرا می‌شود و در نهایت زینت مرد نمایش داده می‌شود.

🛠 پیش‌نیازها

Python 3.x

کتابخانه‌های زیر:

pygame

requests

نصب پیش‌نیازها
pip install pygame requests

▶️ نحوه اجرا

کد را در یک فایل مثل main.py ذخیره کنید.

مطمئن شوید که پایتون و کتابخانه‌های لازم نصب شده‌اند.

در ترمینال یا CMD وارد مسیر فایل شوید و فرمان زیر را اجرا کنید:

python main.py
